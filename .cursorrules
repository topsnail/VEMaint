
# 🚀 车辆管理系统 UI 深度重构指令集

## 1. 核心目标
重构现有 UI，打造一个适配 **1080P (1920x1080)** 屏幕、**高信息密度**、**极致扁平化** 的专业工具类管理后台。

## 2. 全局视觉规范 (Design Tokens)
- **风格**: 彻底扁平化。**严禁使用阴影 (`shadow-none`)**。所有容器、卡片、输入框仅使用 `1px` 的 `border-slate-200` 进行分割。
- **色彩**: 
    - **主色**: `Blue-600` (#2563eb)，用于主要按钮、进度条、激活态文字。
    - **状态色 (Badge)**: 
        - 正常/运行: `bg-emerald-50 text-emerald-700` (绿色系)
        - 预警/维修: `bg-orange-50 text-orange-700` (橙色系)
        - 停用/其他: `bg-slate-100 text-slate-600` (灰色系)
- **圆角**: 全局统一使用 `rounded-sm` (4px)，呈现硬朗、现代的工业感。
- **排版**: 
    - 基础正文: `text-sm` (14px)。
    - 表头、标签、次要说明: `text-xs` (12px)。

## 3. 布局架构 (Layout)
- **TopBar (h-12)**: 
    - 保持 `fixed` 或 `sticky` 置顶。
    - **集成搜索**: 将全局车辆搜索框嵌入 TopBar 中间偏左位置，尺寸 `h-8 w-64`。
    - 背景白色，仅保留 `border-b border-slate-200`。
- **Sidebar (w-52)**: 
    - 背景 `bg-slate-50`，右侧边框 `border-r border-slate-200`。
    - 导航项高度紧凑，激活态：`text-blue-600 bg-blue-50 font-medium`。
- **Main Content**: 背景白色，全局内边距 `p-4`。

## 4. 关键组件重构规范
- **单页长列表表格 (Table)**:
    - **取消分页**: 50 辆车直接单页全量展示。
    - **表头吸顶 (Sticky Header)**: 滚动时表头固定在 TopBar 下方，背景色 `bg-slate-50`。
    - **斑马纹**: 偶数行使用 `bg-slate-50/50`。
    - **尺寸**: 单元格内边距 `py-2 px-3`，确保单屏承载更多数据。
    - **操作列**: 最右侧仅显示 `MoreHorizontal` (...) 图标，点击弹出 `DropdownMenu`。菜单内**仅包含**：“编辑”与“删除”。
- **状态标签 (Badge)**: 
    - 采用“矩形微圆角 + 浅色背景”，取消边框，字体 `font-medium`。
- **独立表单页 (Standalone Form Page)**:
    - 彻底放弃所有 Modal/Dialog 弹窗表单。
    - 新增和编辑车辆均跳转至独立路由页面。
    - 布局采用 `grid-cols-2`，确保在 1080P 下表单紧凑且易读。

## 5. 技术执行指南
- **组件微调**: 修改 `components/ui/` 目录下 shadcn 组件源码：
    - `Button`: 将默认高度从 `h-10` 改为 `h-8` 或 `h-9`。
    - `Input`: 将默认高度改为 `h-8`。
- **图标**: 统一使用 `Lucide React`，尺寸 `w-4 h-4`，线条粗细 `strokeWidth={1.5}`。
- **状态管理**: 继续使用 `TanStack Query` 处理数据，`Sonner` 处理右下角通知。
- **响应式**: 虽然以 1080P 为主，但需确保移动端基本可用（Sidebar 可隐藏）。

---

### 💡 执行建议 
你可以分三步

1. **第一步（基础设施）**：“请根据指令集修改 tailwind 变量和 components/ui 下的基础组件样式，特别是高度和圆角。”
2. **第二步（布局）**：“重构 AppLayout，实现 TopBar 搜索集成和紧凑型 Sidebar。”
3. **第三步（核心业务）**：“重构车辆列表页，实现单页长列表、吸顶表头和斑马纹表格，并把操作收纳进‘...’菜单。”