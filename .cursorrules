# 技术栈约束
- Framework: Next.js (App Router)
- Deployment: Cloudflare Pages (Edge Runtime)
- Database: Cloudflare D1 (Drizzle ORM)
- Storage: R2 (Images/PDFs), KV (Settings)
- UI: Tailwind CSS, Shadcn/ui, Lucide Icons

# UI 风格指南
- 风格: 极简主义, 模仿 QuickNote.io
- 视觉: 玻璃拟态 (Glassmorphism), 浅色模式优先
- 交互: 抽屉式详情 (Sheet), 响应式卡片流

# 编码规范
- 优先使用 Server Actions 进行数据库操作。
- 严禁使用 Node.js 原生 API (如 fs, path)。
- 所有代码必须兼容 Cloudflare Workers 运行时。
- D1 操作必须通过 Drizzle ORM。